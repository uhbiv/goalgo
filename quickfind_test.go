package goalgo_test

import (
	"testing"
)

func TestQuickFind(t *testing.T) {

	var x, y int
	x = 5
	y = x + 9
	y += 1
	// var componentobjects []int
	// var componentCount int = len(componentobjects)
	//int N = StdIn.readInt();
	//N := 0 //TODO: read first line
	//QuickFindUF uf = new QuickFindUF(N);
	//QuickFindUF(N)
	//while { //TODO: input stream
	//int p = StdIn.readInt();
	//int q = StdIn.readInt();
	// if (uf.connected(p, q))  {
	// 	continue
	// }
	// uf.union(p, q);
	// StdOut.println(p + " " + q);
	//}
	//StdOut.println(uf.count() + " components");
	// for i := 0; i < componentCount; i++ {
	// 	//obtain connected objects through parsing string as numbers
	// 	//objects =
	// 	componentobjects := make([]int, 3)
	// 	// for j := range componentobjects {

	// 	// }
	// }
	t.Fatalf("fatally wrong")
	t.Errorf("erroneous")
}
